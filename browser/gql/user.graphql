query CurrentUser {
  currentUser {
    name
    email
    picture
    id: userId
    createdAt
    updatedAt
    theme
  }
}

mutation updateTheme($theme: ThemePreference!) {
  updateUserTheme(theme: $theme) {
    theme
  }
}

subscription theme {
  userTheme
}
