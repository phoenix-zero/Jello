query CurrentUser {
  currentUser {
    name
    email
    picture
    id: userId
    createdAt
    updatedAt
  }
}

mutation Test($name: String) {
  updateUser(user: { name: $name }) {
    name
  }
}
